{
  "name": "confit-mcp-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Remote MCP server on Cloudflare Workers — proxies to CONFIT FastAPI (all /api + style-dna, planner, sustainability)",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.26.0",
    "agents": "^0.5.0",
    "zod": "^4.3.6"
  },
  "overrides": {
    "@modelcontextprotocol/sdk": "1.26.0"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "wrangler": "^4.67.0"
  }
}

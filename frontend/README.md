# Frontend (CONFIT)

The React/Vite frontend lives in `src/` (this repo’s canonical UI root).

This `frontend/` directory exists only to match the requested layout and avoid maintaining a duplicated React tree.

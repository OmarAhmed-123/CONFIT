'use client';

/**
 * Client-only entry for the try-on studio (same module as the legacy Vite page).
 * Used by `app/try-on/page.tsx` with `dynamic(..., { ssr: false })`.
 */
export { default } from '@/pages/VirtualTryOn';
